<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="assignments.ex5.model.mapper.BookMapper">
    <select id="getMoreThanAverageStock" resultType="assignments.ex5.model.dto.BookDto">
        select * from books where stock > (select avg(stock) from books)
    </select>

    <select id="getMostRentedBook" resultType="assignments.ex5.model.dto.BookDto">
        select * from books where id = (select book_id from rentals group by book_id order by count(*) desc limit 1);
    </select>
</mapper>